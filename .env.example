# ========================================
# CONFIGURAÇÃO DO SISTEMA DE INVESTIGAÇÃO
# ========================================
# 
# IMPORTANTE: Copie este arquivo para .env e preencha com valores reais
# NUNCA commite o arquivo .env no Git (já está no .gitignore)
#

# --- AMBIENTE ---
# Opções: development, production, testing
FLASK_ENV=development

# --- SEGURANÇA CRÍTICA ---
# Secret key para sessões do Flask (OBRIGATÓRIO EM PRODUÇÃO)
# Gere com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Chave para criptografia de dados sensíveis no banco (OBRIGATÓRIO EM PRODUÇÃO)
# Gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# --- AUTENTICAÇÃO ---
# Credenciais do administrador do sistema
ADMIN_USER=admin
ADMIN_PASSWORD=

# --- BANCO DE DADOS ---
# SQLite (desenvolvimento):
# DATABASE_URL=sqlite:///instance/dados.db
#
# PostgreSQL (produção recomendada):
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/nome_do_banco
#
# MySQL (alternativa):
# DATABASE_URL=mysql+pymysql://usuario:senha@localhost:3306/nome_do_banco
DATABASE_URL=sqlite:///instance/dados.db

# --- LOGS ---
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# --- SERVIDOR (PRODUÇÃO) ---
# Porta do servidor
PORT=5000

# Host (0.0.0.0 para aceitar conexões externas)
HOST=0.0.0.0

# Número de workers do Gunicorn
WORKERS=4

# --- RATE LIMITING (Proteção contra força bruta) ---
# Formato: "X por Y" onde X é número e Y é período (second, minute, hour, day)
RATELIMIT_DEFAULT=100 per hour
RATELIMIT_LOGIN=5 per minute

# --- NOTIFICAÇÕES (OPCIONAL) ---
# Email para receber alertas quando novos dados forem coletados
# ALERT_EMAIL=
# SMTP_SERVER=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
